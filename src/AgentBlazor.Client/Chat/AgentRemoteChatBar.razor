<div class="ab-remote-bar @CssClass" style="@Style" data-testid="agent-remote-chat-bar">
    <AgentRemoteChatSurface Endpoint="@Endpoint"
                            AgentName="@AgentName"
                            SessionId="@SessionId"
                            UserId="@UserId"
                            Context="@Context"
                            Title="@Title"
                            Placeholder="@Placeholder"
                            EmptyText="@EmptyText" />
</div>

@code {
    [Parameter] public string Endpoint { get; set; } = "/agentblazor/chat/run";
    [Parameter] public string? AgentName { get; set; }
    [Parameter] public string SessionId { get; set; } = $"remote-bar-{Guid.NewGuid():N}";
    [Parameter] public string? UserId { get; set; }
    [Parameter] public IReadOnlyDictionary<string, string>? Context { get; set; }
    [Parameter] public string Title { get; set; } = "Assistant";
    [Parameter] public string Placeholder { get; set; } = "Ask the assistant...";
    [Parameter] public string EmptyText { get; set; } = "Start a remote AgentBlazor chat.";
    [Parameter] public string? CssClass { get; set; }
    [Parameter] public string? Style { get; set; }
}
