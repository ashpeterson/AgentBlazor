{
  "name": "@agentblazor/e2e",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test:e2e": "playwright test --config=playwright.config.cjs",
    "test:e2e:headed": "playwright test --config=playwright.config.cjs --headed",
    "test:e2e:ui": "playwright test --config=playwright.config.cjs --ui",
    "install:browsers": "playwright install --with-deps chromium",
    "test:real-usability": "node ./scripts/real-usability-runner.cjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
